$ git init

$ git add . //all file adding
$ git add abc.txt  //single file adding

git commit -m "React PHP-Api-Mis-Angular-React "

$ git remote add origin "http://github.com:msr2royal/Git-Data-Camp-Tutorial.git"

$ git config --global user.name "msr2royal"
$ git config --global user.email "msr2royal@gmail.com"
            //login into github with credentials

$ git push origin master
